# WebSocket Server Setup - Инструкция по запуску

## Описание
WebSocket сервер для real-time транскрибации с распознаванием спикеров и голосовыми персонажами (10 режимов).

**Файл:** `websocket_server_with_speakers.py`

## Быстрый старт

### 1. Создание виртуального окружения (рекомендуется)
```powershell
# Создать виртуальное окружение
python -m venv venv_websocket

# Активировать
.\venv_websocket\Scripts\Activate.ps1

# Если ошибка ExecutionPolicy, выполните:
Set-ExecutionPolicy -ExecutionPolicy RemoteSigned -Scope CurrentUser
```

### 2. Установка зависимостей
```powershell
# Установить специфичные зависимости для WebSocket сервера
pip install -r requirements_websocket_server.txt
```

### 3. Запуск сервера
```powershell
python websocket_server_with_speakers.py
```

## Что произойдет при первом запуске

1. **Загрузка моделей** (автоматически):
   - Faster-Whisper Small (~140MB) - для транскрибации
   - SpeechBrain ECAPA-TDNN (~200MB) - для распознавания голосов
   - pyttsx3 (0MB) - локальный TTS движок

2. **Сервер запустится** на `ws://0.0.0.0:8765`

3. **Консоль покажет**:
   ```
   🚀 ЗАПУСК WEBSOCKET СЕРВЕРА С РАСПОЗНАВАНИЕМ СПИКЕРОВ
   ✅ Faster-Whisper загружен
   ✅ SpeechBrain загружен
   🎭 Голосовой персонаж инициализирован: normal
   ⚡ WebSocket сервер запущен на ws://0.0.0.0:8765
   ```

## Использование

### Запуск клиента
```powershell
# В отдельном терминале
python voice_client.py
```

### Голосовые команды
После подключения клиента можно использовать голосовые команды:
- "режим robot" → переключиться в режим робота
- "список режимов" → показать все доступные режимы
- "как дела?" → пасхалка
- "расскажи шутку" → случайная шутка
- "спой песню" → поёт что-то смешное

### Доступные режимы (10 штук)
- `normal` - обычный голос
- `robot` 🤖 - металлический голос робота
- `soft` 🧸 - мягкий плюшевый с уменьшительными
- `sarcastic` 😎 - ироничный саркастичный
- `wizard` 🧙 - архаичный магический
- `cat` 🐱 - мяукающий кот
- `happy` 🎉 - радостный энергичный
- `tired` 😑 - уставший сонный
- `whisper` 🤫 - тихий шёпот
- `professor` 📚 - умный научный

Подробнее: `VOICE_MODES.md`

## Регистрация спикеров

Сервер автоматически распознает голоса. Для регистрации нового голоса:

```python
# В клиенте отправить JSON:
{
    "type": "register_speaker",
    "speaker_name": "Иван",
    "audio": "<base64_encoded_audio>"
}
```

База спикеров сохраняется в `speakers_database.pkl`.

## Решение проблем

### Ошибка: AttributeError: module 'torchaudio' has no attribute 'list_audio_backends'
**Причина:** Несовместимость версий `torchaudio` и `speechbrain`

**Решение:**
```powershell
# Удалить старые версии
pip uninstall torch torchaudio speechbrain -y

# Установить совместимые версии
pip install torch==2.1.0 torchaudio==2.1.0 speechbrain==0.5.16
```

### Ошибка: CUDA недоступна
**Не критично!** Сервер будет работать на CPU (медленнее, но работает).

Для включения CUDA:
1. Установите CUDA Toolkit 11.8
2. Установите PyTorch с CUDA:
   ```powershell
   pip install torch==2.1.0+cu118 torchaudio==2.1.0+cu118 --index-url https://download.pytorch.org/whl/cu118
   ```

### Ошибка: pyttsx3 не работает на Windows
```powershell
pip install --upgrade pywin32
python -m pywin32_postinstall -install
```

### Порт 8765 занят
Измените порт в `websocket_server_with_speakers.py`:
```python
# Строка в конце файла
await websockets.serve(handle_client, "0.0.0.0", 8765)
# Поменяйте 8765 на любой свободный порт
```

## Системные требования

- **Python:** 3.8 - 3.12 (рекомендуется 3.10)
- **ОС:** Windows 10/11 (pyttsx3 использует SAPI5)
- **Память:** минимум 4GB RAM
- **Диск:** ~500MB для моделей
- **Микрофон:** для клиента
- **GPU:** опционально (CUDA ускоряет в ~5 раз)

## Архитектура

```
┌─────────────────┐         WebSocket          ┌─────────────────┐
│  voice_client   │ ──────────────────────────> │  WebSocket      │
│   (микрофон)    │ <────────────────────────── │     Server      │
└─────────────────┘                             └─────────────────┘
                                                         │
                                    ┌────────────────────┼────────────────────┐
                                    │                    │                    │
                              ┌─────▼─────┐     ┌───────▼───────┐    ┌──────▼──────┐
                              │  Faster-  │     │  SpeechBrain  │    │    Voice    │
                              │  Whisper  │     │    ECAPA      │    │ Personality │
                              │ (transcr) │     │  (speakers)   │    │   (TTS)     │
                              └───────────┘     └───────────────┘    └─────────────┘
```

## Протокол WebSocket

### Клиент → Сервер

#### Отправка аудио
```json
{
    "type": "audio",
    "audio": "<base64_encoded_pcm_int16>",
    "sample_rate": 16000,
    "enable_speaker_recognition": true  // опционально, по умолчанию true
}
```

**Параметр `enable_speaker_recognition`:**
- `true` (по умолчанию) - включено распознавание спикеров (полная функциональность)
- `false` - отключено распознавание спикеров (работает как обычный websocket_server.py, быстрее)

#### Финализация транскрибации
```json
{
    "type": "finalize"
}
```

#### Регистрация нового спикера
```json
{
    "type": "register_speaker",
    "name": "Иван",
    "audio": "<base64_encoded_audio>"
}
```

#### Список спикеров
```json
{
    "type": "list_speakers"
}
```

#### Голосовые команды
```json
{
    "type": "voice_command",
    "command": "set_mode",
    "mode": "robot"
}
```

### Сервер → Клиент

#### С распознаванием спикеров (enable_speaker_recognition: true)
```json
{
    "type": "transcription",
    "text": "распознанный текст",
    "is_final": true,
    "timestamp": "2024-11-16T12:34:56",
    "speaker_recognition_enabled": true,
    "speaker": {
        "id": "speaker_1",
        "name": "Иван",
        "similarity": 0.85,
        "is_known": true
    },
    "voice_response": "processed",
    "current_voice_mode": "robot",
    "metrics": {
        "transcription_time_ms": 150.25,
        "transcription_time_s": 0.150,
        "speaker_recognition_time_ms": 45.30,
        "audio_duration_s": 3.5,
        "realtime_factor": 1.23,
        "samples": 56000
    }
}
```

#### Без распознавания спикеров (enable_speaker_recognition: false)
```json
{
    "type": "transcription",
    "text": "распознанный текст",
    "is_final": true,
    "timestamp": "2024-11-16T12:34:56",
    "speaker_recognition_enabled": false,
    "voice_response": "processed",
    "current_voice_mode": "normal",
    "metrics": {
        "transcription_time_ms": 150.25,
        "transcription_time_s": 0.150,
        "audio_duration_s": 3.5,
        "realtime_factor": 1.23,
        "samples": 56000
    }
}
```

## Файлы проекта

- **`websocket_server_with_speakers.py`** - основной сервер
- **`voice_personality.py`** - система голосовых персонажей
- **`voice_client.py`** - клиент с VAD
- **`test_tts.py`** - быстрое тестирование TTS
- **`requirements_websocket_server.txt`** - зависимости
- **`VOICE_MODES.md`** - документация режимов
- **`speakers_database.pkl`** - база голосов (создаётся автоматически)

## Логирование

Сервер выводит:
- 🎙 Подключение клиента
- 📊 Размер полученных данных
- 🧠 Результат транскрибации
- 🎭 Смена голосового режима
- 👤 Идентификация спикера
- ⚡ RTF (Real-Time Factor)

## Дополнительные команды

### Просмотр списка спикеров
```python
# В клиенте
{"type": "list_speakers"}
```

### Тестирование TTS без сервера
```powershell
python test_tts.py          # интерактивный режим
python test_tts.py all      # все режимы подряд
```

## Известные проблемы

1. **Python 3.13**: speechbrain может некорректно работать → используйте Python 3.10-3.12
2. **Виртуальное окружение**: настоятельно рекомендуется для избежания конфликтов
3. **Первый запуск**: загрузка моделей занимает 2-5 минут

## Поддержка

Все логи сохраняются в консоли. При проблемах:
1. Проверьте версии: `pip list | Select-String "torch|speechbrain|whisper"`
2. Попробуйте переустановить зависимости
3. Проверьте Python версию: `python --version`
