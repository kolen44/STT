python -m venv venv && source venv/bin/activate
pip install --upgrade pip
pip install websockets numpy soundfile transformers optimum[onnxruntime] onnxruntime-gpu sentencepiece
# (если возникают проблемы с onnxruntime-gpu, ставь версию совместимую с твоей CUDA)
